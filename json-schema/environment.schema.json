{
  "$id": "https://schema.workspec.io/v1alpha1/environment.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A WorkSpec environment artifact: naming conventions and per-resource overrides.",
  "properties": {
    "apiVersion": {
      "const": "workspec.io/v1alpha1",
      "description": "Artifact API version discriminant.",
      "type": "string"
    },
    "kind": {
      "const": "Environment",
      "description": "Artifact kind discriminant.",
      "type": "string"
    },
    "metadata": {
      "description": "Common artifact identity.",
      "properties": {
        "slug": {
          "description": "Stable filename slug. Optional: the loader derives it from the artifact's filename under `.workspec/<kind-dir>/<slug>.yaml` when absent; when present it must already be a valid slug.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "type": "object"
    },
    "spec": {
      "description": "The environment body: naming conventions and per-resource override patches.",
      "properties": {
        "naming": {
          "description": "Naming conventions for this environment.",
          "properties": {
            "resourceGroupSuffix": {
              "description": "Suffix appended to generated resource-group names in this environment.",
              "minLength": 1,
              "type": "string"
            }
          },
          "type": "object"
        },
        "overrides": {
          "additionalProperties": {
            "description": "A deep-merge patch over one resource, scoped to this environment.",
            "properties": {
              "config": {
                "additionalProperties": {},
                "description": "Patch merged over the resource's own `spec.config` bag.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "cost": {
                "description": "Patch merged over the resource's own `spec.cost` fields.",
                "properties": {
                  "attribution": {
                    "description": "Override the cost attribution split.",
                    "items": {
                      "description": "Attributes a share of a resource's cost to a c4 container.",
                      "properties": {
                        "container": {
                          "description": "The c4 container slug this share is attributed to.",
                          "minLength": 1,
                          "type": "string"
                        },
                        "share": {
                          "description": "Fraction of the resource cost attributed to this container.",
                          "maximum": 1,
                          "minimum": 0,
                          "type": "number"
                        }
                      },
                      "required": [
                        "container",
                        "share"
                      ],
                      "type": "object"
                    },
                    "type": "array"
                  },
                  "mode": {
                    "description": "Override the bound catalog pricing mode id.",
                    "minLength": 1,
                    "type": "string"
                  },
                  "qty": {
                    "description": "Override the unit count.",
                    "maximum": 9007199254740991,
                    "minimum": 0,
                    "type": "integer"
                  },
                  "schedule": {
                    "description": "Override the bound catalog schedule id.",
                    "minLength": 1,
                    "type": "string"
                  },
                  "sku": {
                    "description": "Override the bound catalog SKU id.",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "type": "object"
              }
            },
            "type": "object"
          },
          "description": "Per-resource deep-merge patches, keyed by resource slug. A key for a resource not present in this environment is schema-valid; resolving it is a `verify`-time concern.",
          "propertyNames": {
            "description": "A WorkSpec slug: lowercase alphanumeric segments separated by single hyphens, at most 64 characters (the shape `slugify()` produces).",
            "maxLength": 64,
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "type": "string"
          },
          "type": "object"
        }
      },
      "type": "object"
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "WorkSpec Environment (v1alpha1)",
  "type": "object"
}
