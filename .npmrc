auto-install-peers=true
strict-peer-dependencies=false
# Only the `workspace:` protocol links a local package; a plain version range
# (e.g. apps/site's "@workspec/decision-ui": "0.1.0-alpha.0") resolves from the
# registry. This keeps the site a genuine test of the PUBLISHED packages.
link-workspace-packages=false
