# yaml-language-server: $schema=https://schema.workspec.io/v1alpha1/resource.schema.json
apiVersion: workspec.io/v1alpha1
kind: Resource
metadata:
  slug: rg-network-01
spec:
  name: 'Network Resource Group'
  kind: resource-group
  type: 'Azure Resource Group'
  provider: azure
  source:
    kind: derived
    from: 'Microsoft.Resources/resourceGroups:rg-network-01'
