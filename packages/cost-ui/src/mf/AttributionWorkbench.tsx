// Module-federation expose: `./AttributionWorkbench` — the unified 2a
// workbench (rule rail + resource table + cascade + triage/composer). Same
// `AttributionWorkbench` as the lib build. Styles ride along on load.
import '../index.css';
import { AttributionWorkbench } from '../attribution-workbench.js';

export default AttributionWorkbench;
