// Module-federation expose: `./CostReport` — the rollups + cross-tab report.
// Same `CostReport` as the lib build. Styles ride along on load.
import '../index.css';
import { CostReport } from '../cost-report.js';

export default CostReport;
