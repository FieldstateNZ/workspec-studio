// Module-federation expose: `./TagPlanView` — the read-only tag-plan review.
// Same `TagPlanView` as the lib build. Styles ride along on load.
import '../index.css';
import { TagPlanView } from '../tag-plan-view.js';

export default TagPlanView;
