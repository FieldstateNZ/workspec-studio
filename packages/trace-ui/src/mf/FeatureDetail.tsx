// Module-federation expose: `./FeatureDetail` — the same component as the
// lib build. Styles ride along on load (see `./MetersBar.tsx`'s comment).
import '../index.css';
import { FeatureDetail } from '../feature-detail.js';

export default FeatureDetail;
