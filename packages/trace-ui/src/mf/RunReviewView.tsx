// Module-federation expose: `./RunReviewView` — the same component as the
// lib build. Styles ride along on load (see `./MetersBar.tsx`'s comment).
import '../index.css';
import { RunReviewView } from '../run-review-view.js';

export default RunReviewView;
