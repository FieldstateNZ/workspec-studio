// Module-federation expose: `./MatrixView` — the same component as the lib
// build. Styles ride along on load (see `./MetersBar.tsx`'s comment).
import '../index.css';
import { MatrixView } from '../matrix-view.js';

export default MatrixView;
