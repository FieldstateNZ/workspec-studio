// Module-federation expose: `./RequirementsExplorer` — the same component as
// the lib build. Styles ride along on load (see `./MetersBar.tsx`'s comment).
import '../index.css';
import { RequirementsExplorer } from '../requirements-explorer.js';

export default RequirementsExplorer;
