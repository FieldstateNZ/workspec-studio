/** Tag every element `import-aspire` writes carries — the single marker that makes an element "governed" by `check`/`scaffold`. */
export const ASPIRE_MANAGED_TAG = 'aspire-managed';

/** Slug of the one generated diagram (`diagrams/aspire-container.yaml`). */
export const ASPIRE_DIAGRAM_SLUG = 'aspire-container';

/** `source` field value stamped on every element/diagram/system `import-aspire` writes. */
export const ASPIRE_SOURCE_NOTE = 'workspec-c4 import-aspire';
