# yaml-language-server: $schema=https://schema.workspec.io/v1alpha1/environment.schema.json
apiVersion: workspec.io/v1alpha1
kind: Environment
metadata:
  slug: legacy-environment-overrides
spec:
  naming:
    resourceGroupSuffix: '-prod'
  overrides:
    app-service:
      cost:
        qty: 2
