{
  "$id": "https://schema.workspec.io/v1alpha1/system-requirement.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A WorkSpec SystemRequirement artifact.",
  "properties": {
    "apiVersion": {
      "const": "workspec.io/v1alpha1",
      "description": "Artifact API version discriminant.",
      "type": "string"
    },
    "kind": {
      "const": "SystemRequirement",
      "description": "Artifact kind discriminant.",
      "type": "string"
    },
    "metadata": {
      "description": "Common artifact identity.",
      "properties": {
        "slug": {
          "description": "Stable filename slug. Optional: the loader derives it from the artifact's filename under `.workspec/<kind-dir>/<slug>.yaml` when absent; when present it must already be a valid slug.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "type": "object"
    },
    "spec": {
      "description": "A system-requirement: a Gherkin Rule. Groups the scenarios that prove it; has no steps of its own.",
      "properties": {
        "feature": {
          "description": "Bare-slug intra-tree ref → features/*: the containing feature.",
          "maxLength": 64,
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "type": "string"
        },
        "links": {
          "description": "Cross-references to other artifacts or docs: exactly one {<linkType>: <pathRef>} pair per entry, plus an optional cardinality key ({from, to, label?}). Path refs must start with \"~/\" or \"@workspace/\".",
          "items": {
            "additionalProperties": {},
            "propertyNames": {
              "description": "The link type, e.g. \"adr\", \"runbook\" — or \"cardinality\" for the optional cardinality key.",
              "type": "string"
            },
            "type": "object"
          },
          "type": "array"
        },
        "title": {
          "description": "One-line summary of the requirement this Rule states.",
          "minLength": 1,
          "type": "string"
        },
        "userReqs": {
          "description": "Bare-slug intra-tree refs → requirements/user/*: the user-requirement(s) this Rule verifies (the \"verifies\" edge; at least one).",
          "items": {
            "description": "A WorkSpec slug: lowercase alphanumeric segments separated by single hyphens, at most 64 characters (the shape `slugify()` produces).",
            "maxLength": 64,
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "title",
        "feature",
        "userReqs"
      ],
      "type": "object"
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "WorkSpec System Requirement (v1alpha1)",
  "type": "object"
}
