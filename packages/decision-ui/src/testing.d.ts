// Make the jest-dom matchers (`toBeInTheDocument`, `toHaveAttribute`, …) visible
// to TypeScript by pulling in their Vitest `expect` augmentation. Runtime
// registration happens in `vitest.setup.ts`.
import '@testing-library/jest-dom/vitest';
