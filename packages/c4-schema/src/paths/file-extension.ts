/**
 * The one and only file extension WorkSpec artifact files use. Enterprise
 * conformance note: this is `.yaml`, never `.yml` — a tree containing
 * `*.yml` files is not a valid WorkSpec tree.
 */
export const FILE_EXTENSION = '.yaml';
