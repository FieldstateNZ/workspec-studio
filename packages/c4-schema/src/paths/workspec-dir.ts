/**
 * Root directory of a WorkSpec working tree, relative to the repository
 * root. Every artifact family (C4 elements, diagrams, decisions, etc.)
 * lives under this one directory.
 */
export const WORKSPEC_DIR = '.workspec';
