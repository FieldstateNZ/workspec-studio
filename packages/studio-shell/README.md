# @workspec/studio-shell

Enterprise-derived WorkSpec application chrome for composed Studio workflows. It provides the
header, WebMCP status, collapsible left navigation, workflow steps, import and download actions;
domain content remains owned by the package mounted inside it.
