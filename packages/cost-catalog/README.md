# @workspec/cost-catalog

Provider-agnostic reference-data schemas and a pure pricing engine for WorkSpec solution options.
Providers own resources and SKUs; pricing models may be shared or provider-specific; solution options
reference those records without assuming a fixed cloud vendor.
