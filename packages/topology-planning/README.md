# @workspec/topology-planning

Provider-neutral infrastructure planning for WorkSpec. It derives editable requirements and
traceability links from C4 deployable elements, compares deterministic Azure and AWS mappings,
and materializes schema-valid Environment, Resource, Catalog, and Topology artifacts for the
accepted provider.
