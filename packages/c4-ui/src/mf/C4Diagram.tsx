// Module-federation expose: `./C4Diagram` — the same component as the lib build. Styles ride along on load.
import '../index.css';
import { C4Diagram } from '../c4-diagram.js';

export default C4Diagram;
