// Module-federation expose: `./C4Explorer` — the same component as the lib build. Styles ride along on load.
import '../index.css';
import { C4Explorer } from '../c4-explorer.js';

export default C4Explorer;
