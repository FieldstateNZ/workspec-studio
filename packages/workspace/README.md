# @workspec/workspace

Browser-safe, byte-preserving `.workspec` workspace state. Imports a ZIP safely, strips an optional
project root, preserves unknown and binary files, supports text/byte edits below `.workspec/`, and
exports the complete canonical file map as a ZIP.
