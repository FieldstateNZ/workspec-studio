node_modules/
dist/
dist-mf/
build/
coverage/
test-results/
playwright-report/
.turbo/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.vitest-cache/
.typecheck/

# render-adr output (packages/decision-studio) is a generated artifact, never committed
*.adr.md

# .NET build output (aspire-hosting/) — scoped so it can't shadow anything the TS side uses
aspire-hosting/**/bin/
aspire-hosting/**/obj/
aspire-hosting/**/TestResults/

# Aspire CLI's generated TypeScript AppHost SDK (examples/aspire-apphost and any future TS
# AppHosts) — regenerated by `aspire restore`/`aspire run`, never committed.
**/.aspire/
